export interface User {
    id?: Number,
    name?: string,
    username: string,
    password: string,
    role?: string,
    address?: string,
    phone?: string,
    token?: string,
    email?: string
}